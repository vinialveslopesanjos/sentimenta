# API URL (Vite env var — prefix VITE_ obrigatório)
# Dev local: http://192.168.x.x:8000/api/v1  (ou deixe em branco para usar /api/v1 relativo)
# VPS: deixar em branco — usa /api/v1 relativo (nginx proxia para o backend)
VITE_API_URL=/api/v1
